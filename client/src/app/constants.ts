export const AUTHORIZATION_HEADER = "Authorization";
export const BEARER_PREFIX = "Bearer";
export const BASIC_PREFIX = "Basic";
export const DEFAULT_DIALOG_WIDTH = "400px";

export enum ActionTypes {
    SIGN_IN = "Sign In",
    SIGN_OUT = "Sign Out",
    TEST_LINK = "Test Link",
}
